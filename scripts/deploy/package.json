{
  "name": "defi-infrastructure-deploy",
  "version": "1.0.0",
  "scripts": {
    "deploy": "node deploy.js",
    "verify": "node verify.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "ethers": "^6.0.0"
  }
}